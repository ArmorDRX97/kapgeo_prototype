export type InterpretationKind = 'permeable' | 'filtration' | 'differential' | 'ore' | 'tech' | 'manual' | 'ai' | 'resolved'
export type InterpretationInterval = { id: string; kind: InterpretationKind; from: number; to: number; title: string; method: string; source: 'manual' | 'automatic' | 'AI synthetic' | 'adapter'; state: 'draft' | 'proposed' | 'accepted' | 'preliminary' | 'no-correction'; version: number; evidence: string[]; reason?: string; properties?: { ks?: number; filtration?: number; grade?: number } }
export type AiProposal = { id: string; modelVersion: string; confidence: number; calibration: string; intervals: InterpretationInterval[]; status: 'proposed' | 'resolved'; evidence: string[] }
export type InterpretationWorkspace = { wellId: string; permeable: InterpretationInterval[]; filtration: InterpretationInterval[]; differential: InterpretationInterval[]; ore: InterpretationInterval[]; tech: InterpretationInterval[]; manual: InterpretationInterval[]; autoProposal?: InterpretationInterval[]; aiProposal?: AiProposal; resolved?: InterpretationInterval[]; workflow: 'draft' | 'in_review' | 'approved' | 'published'; version: number; updatedAt: string }