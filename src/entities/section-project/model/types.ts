export type SectionProjectWorkspace={id:string;name:string;stage:'draft'|'review'|'published';range:{from:number;to:number};route:Array<{id:string;x:number;y:number}>;wells:Array<{id:string;included:boolean;order:number;distance:number}>;connectivity:Array<{id:string;from:string;to:string;pinchOut:boolean}>;helpers:Array<{id:string;from:number;to:number;scope:'section-only'}>;contours:Array<{id:string;points:number;mode:'bezier'|'straight';snapped:boolean}>;oreBodies:Array<{id:string;type:'balance'|'off-balance'|'preliminary';valid:boolean}>;oxidation:Array<{id:string;closed:boolean;reversed:boolean}>;rhythm:string;version:number;updatedAt:string}