export type ColumnTemplate = { id: string; name: string; tracks: Array<'curves' | 'lithology' | 'construction' | 'samples' | 'ore' | 'labels'>; splitScale: boolean; version: number }
export type OutputDocument = { id: string; name: string; version: number; state: 'active' | 'archived'; source: 'synthetic upload' | 'local demo file' }
export type OutputWorkspace = { wellId: string; template: ColumnTemplate; annotations: Array<{ id: string; depth: number; label: string; source: string }>; documents: OutputDocument[]; map: { crs: string; selectedLayer: 'trajectory' | 'profile' | 'block'; edited: boolean; measureM: number }; savedViews: Array<{ id: string; name: string; layers: string[]; style: string }>; exportRequest: { pages: number; crop: boolean; rotation: number } ; version: number; updatedAt: string }