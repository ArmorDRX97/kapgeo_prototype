export type ReserveMethod='projection'|'voronoi'|'interval-registry'|'geostatistical'
export type ReserveRun={id:string;method:ReserveMethod;tonnage:number;metal:number;status:'succeeded'|'stale';protocol:string;version:number}
export type ReserveProjectWorkspace={id:string;stage:'draft'|'review'|'approved'|'published';snapshotId:string;packages:string[];profiles:string[];intersections:Array<{id:string;wellId:string;mode:'fast'|'normal'|'manual';active:boolean;thickness:number;reason:string}>;blocks:Array<{id:string;type:'balance'|'off-balance';vertices:number;valid:boolean}>;wellSet:Array<{wellId:string;role:'included'|'network';reason:string}>;cells:Array<{id:string;name:string;valid:boolean}>;runs:ReserveRun[];activeRunId?:string;plan:{layers:string[];isolines:boolean;fill:boolean};version:number;updatedAt:string}