export type InterpolationMethod='kriging'|'idw'|'minimum-curvature'|'sibson-laplace'
export type GeologicalModelWorkspace={id:string;snapshotId:string;domain:{regions:number;axes:string;faults:number;gridX:number;gridY:number};points:Array<{id:string;x:number;y:number;value:number;included:boolean}>;analysis:{mean:number;std:number;rejected:string[]};variogram:{direction:number;lag:number;tolerance:number;bandwidth:number;model:'spherical'|'exponential';anisotropy:number};crossValidation:{rmse:number;bias:number;accepted:boolean};runs:Array<{id:string;method:InterpolationMethod;status:'succeeded'|'stale'|'cancelled';coverage:number;noData:number;parameters:string}>;field?:{id:string;runId:string;status:'draft'|'accepted';quality:string};contours:Array<{id:string;threshold:number;kind:'environment'|'ore'}>;view:{mode:'2d'|'volume'|'table';lockedScale:boolean};version:number;updatedAt:string}