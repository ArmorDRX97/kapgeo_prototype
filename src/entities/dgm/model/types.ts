export type DgmWorkspace={id:string;horizons:Array<{id:string;order:number;group:'permeable'|'impermeable'|'custom';constraint:string}>;picks:Array<{id:string;wellId:string;fraction:number;pinchOut:boolean}>;surfaces:Array<{id:string;kind:'roof'|'thickness';nodes:number;conflicts:number}>;mesh:{planNodes:number;triangles:number;prisms:number;groups:number;maxEdge:number;clipped:boolean};analysis:{ellipsoid:string;variogram:string};fields:Array<{id:string;quantity:'content'|'tech-type';group:string;status:'accepted'}>;composition:Array<{group:string;fieldId:string}>;slice:{axis:'plan'|'section';index:number;average:number;integral:number};renderer:'webgl'|'2d'|'table';stage:'draft'|'published';version:number;updatedAt:string}